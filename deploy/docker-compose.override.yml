services:
  frame-up-order-app:
    environment:
        RabbitMQ__Host: rabbitmq
        RabbitMQ__User: guest
        RabbitMQ__Password: guest
        ASPNETCORE_ENVIRONMENT: Development
        ASPNETCORE_HTTP_PORTS: 8080

  # frame-up-gateway-app:
  #   environment:
  #       ASPNETCORE_ENVIRONMENT: Development
  #       ASPNETCORE_HTTP_PORTS: 8080
        
  # frame-up-db:
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres